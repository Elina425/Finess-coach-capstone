# QEVD-FIT-COACH Pose Estimation Pipeline Requirements

# Core Dependencies
numpy>=1.21.0
pandas>=1.3.0
opencv-python>=4.5.0

# Pose Estimation Models
# MediaPipe (lightweight, real-time)
mediapipe>=0.8.0

# YOLOv11 (accurate, modern)
ultralytics>=8.0.0
torch>=1.9.0
torchvision>=0.10.0

# RTMPose-X / ViTPose (RTMPose-m) / DETRPose (RTMO) via rtmlib — ONNXRuntime backends
onnxruntime>=1.16.0
rtmlib>=0.0.13

# OpenPose (most accurate, optional - requires separate system installation)
# See: https://github.com/CMU-Perceptual-Computing-Lab/openpose

# Data Processing
scipy>=1.7.0
scikit-learn>=0.24.0
PyWavelets>=1.3.0  # For DWT normalization

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0
Pillow>=8.0.0

# System Monitoring
psutil>=5.8.0

# JSON/Data serialization
jsonlines>=2.0.0
ijson>=3.2.0

# Optional: Kaggle datasets (e.g. gym IMU CSV pipeline)
# kagglehub>=0.2.0

# Optional: GPU acceleration
# For NVIDIA GPU support (install one of these)
# tensorflow[and-cuda]>=2.8.0  # For TensorFlow backend
# jax[cuda11_cudnn82]>=0.3.0   # For JAX backend
